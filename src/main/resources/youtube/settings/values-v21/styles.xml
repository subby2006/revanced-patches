<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.YouTube.Settings" parent="@style/Base.Theme.YouTube.Settings">
        <item name="android:windowLightStatusBar">true</item>
        <item name="ytStatusBarBackground">@android:color/transparent</item>
        <item name="android:listDivider">@null</item>
    </style>
    <style name="Theme.YouTube.Settings.Dark" parent="@style/Base.Theme.YouTube.Settings.Dark">
        <item name="android:windowLightStatusBar">false</item>
        <item name="ytStatusBarBackground">@android:color/transparent</item>
        <item name="android:listDivider">@null</item>
    </style>
</resources>
